<% content_for :search_metas do %>
  <!-- General meta info + SEO -->
  <title><%= "add a product | #{Setting.first.try :site_name}" %></title>
  <meta name="description" content="This is where you add new products to the online store." />
<% end %>
<!-- original system -->
<% content_for :meta_title, "add a product" %>

<%= render 'tab_admin_menu' %>

<%= render 'form' %>
<%= render 'shared/admin_sidebar' %>
