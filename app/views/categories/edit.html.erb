<% content_for :search_metas do %>
  <!-- General meta info + SEO -->
  <title><%= "edit #{@category.name} | #{Setting.first.try :site_name}" %></title>
  <meta name="description" content="This is where the store staff edits a product category." />
<% end %>
<!-- original system -->
<% content_for :meta_title, "edit #{@category.name} | #{Setting.first.try :site_name}" %>

<%= render 'tab_admin_menu' %>

<%= render 'form' %>

<%= render 'shared/admin_sidebar' %>
